package Array;
import java.util.Arrays;
//To be done
public class LeftRotatByD
{
    public static void main(String[] args)
    {
        int [] ipt = {10, 20, 36, 40, 50};
        int [] temp = {10, 20} ;
        int [] opt = new int[ipt.length] ;
        /*
        System.arraycopy(temp,ipt,0,0,2);
        System.arraycopy(ipt,opt,2,0,ipt.length);
        System.arraycopy(temp,opt,ipt.length-temp.length,ipt.length);
        System.out.println(Arrays.toString(opt));

         */
    }
}
